import * as colors from './colors.json';
import * as color from './color.json';
import * as themes from './themes.json';
import * as theme from './theme-response.json';
import * as minifigs from './minifigs.json';
import * as minifig from './minifig.json';

export namespace DbMocks {
  export const Colors = colors;
  export const Color = color;
  export const Themes = themes;
  export const Theme = theme;
  export const Minifigs = minifigs;
  export const Minifig = minifig;
}
